// Barrel export for client hooks. Add new hooks here so consumers import from
// "@/hooks" with a single, stable path.
export { useToast } from "./use-toast";
export { useIsMobile } from "./use-mobile";
