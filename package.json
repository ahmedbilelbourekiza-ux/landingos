{
  "name": "landingos",
  "version": "3.0.0",
  "description": "LandingOS — a modular SaaS platform. Shared platform services plus independently subscribable products.",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "erp": "npm run --workspace @landingos/erp",
    "erp:start": "npm run start --workspace @landingos/erp",
    "erp:dev": "npm run dev --workspace @landingos/erp",
    "builder:build": "npm run build --workspace @landingos/website-builder",
    "builder:dev": "npm run dev --workspace @landingos/website-builder",
    "builder:start": "npm run start --workspace @landingos/website-builder",
    "test": "npm run test --workspaces --if-present",
    "build": "npm run build --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  },
  "//allowScripts": "npm ignores allowScripts declared inside a workspace and warns about it — it is only honoured at the workspace root. better-sqlite3 needs its install script to fetch (or build) the native binding, so leaving this in apps/erp/package.json would break a clean install with no error until the first require().",
  "allowScripts": {
    "better-sqlite3@12.11.1": true
  }
}
