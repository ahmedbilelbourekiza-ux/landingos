{
  "name": "@landingos/ui",
  "version": "1.0.0",
  "description": "The console design system. One fixed identity for every product's dashboard — deliberately not the storefront's.",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./tokens.css": "./src/tokens.css"
  },
  "scripts": {
    "test": "node --test \"test/*.test.ts\""
  },
  "engines": { "node": ">=22.6" }
}
