# Placeholder connection string for offline validation — not a secret, but
# .env files stay out of git as a rule, without exception.
.env

# Generated Prisma client — rebuilt from the schema by `npm run generate`.
prisma/client/
